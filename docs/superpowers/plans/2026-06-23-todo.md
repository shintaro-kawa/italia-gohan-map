# 旅行 ToDo リスト機能 — 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 旅行前準備タスクを整理・追跡し、PC ↔ スマホで同期できる ToDo リスト機能を `/todo` ページに追加する。既存 D-031 (旅程 sync) の設計パターン・インフラを最大限再利用。

**Architecture:** 旅程と同じ Private Gist (`ITINERARY_GIST_ID`) 内の別ファイル `todos.json` を Source of Truth とする。`/api/sync-todos` (GET + POST) で last-write-wins 同期、localStorage をオフラインキャッシュ + 未送信キューとして使用。UI は新規ページ `/todo`、都市別グルーピング + フィルタチップ。

**Tech Stack:** Astro 4 (static), TypeScript strict (NodeNext), Vercel Functions (@vercel/node), @octokit/rest, native localStorage / fetch / online events. 既存 D-031 と同じ build-verify サイクル (`pnpm build`)、純関数は手書き Node スクリプトで verify。

**Reference Spec:** [docs/superpowers/specs/2026-06-23-todo-design.md](../specs/2026-06-23-todo-design.md)
**Reference Implementation Pattern:** [docs/superpowers/plans/2026-06-18-itinerary-sync.md](2026-06-18-itinerary-sync.md) (旅程 sync) と多くの並行構造

---

## Task 1: Todo 型定義

**Files:**
- Create: `src/types/todo.ts`

- [ ] **Step 1: 新規ファイル作成**

`src/types/todo.ts`:

```ts
export type TodoCity = 'Rome' | 'Florence' | 'Palermo' | 'Taormina' | 'Sicily' | '全般';

export const TODO_CITIES: ReadonlyArray<{ value: TodoCity; label: string }> = [
  { value: 'Rome', label: 'ローマ' },
  { value: 'Florence', label: 'フィレンツェ' },
  { value: 'Palermo', label: 'パレルモ' },
  { value: 'Taormina', label: 'タオルミーナ' },
  { value: 'Sicily', label: 'シチリア' },
  { value: '全般', label: '全般' },
];

export const TODO_CITY_SET = new Set<TodoCity>(TODO_CITIES.map((c) => c.value));

export interface Todo {
  id: string;
  title: string;
  done: boolean;
  city?: TodoCity;
  notes?: string;
  /** ISO 日付 (例: "2026-07-15")、時刻なし */
  deadline?: string;
  /** ISO 8601 */
  createdAt: string;
  /** ISO 8601、同期競合解決に使用 */
  updatedAt: string;
  /** ISO 8601、論理削除のトゥームストーン */
  deletedAt?: string;
}

export function todoCityLabel(city?: TodoCity): string {
  if (!city) return '全般';
  return TODO_CITIES.find((c) => c.value === city)?.label ?? city;
}

/**
 * 期限の緊急度を判定。
 * - overdue: 期限が今日より前
 * - soon: 期限が今日から 7 日以内
 * - normal: 期限が 8〜30 日以内
 * - far: 期限が 30 日より先
 * - none: 期限なし
 */
export type DeadlineUrgency = 'overdue' | 'soon' | 'normal' | 'far' | 'none';

export function deadlineUrgency(deadline: string | undefined, today: Date = new Date()): DeadlineUrgency {
  if (!deadline) return 'none';
  const d = new Date(deadline);
  if (!Number.isFinite(d.getTime())) return 'none';
  const startOfToday = new Date(today.getFullYear(), today.getMonth(), today.getDate()).getTime();
  const target = new Date(d.getFullYear(), d.getMonth(), d.getDate()).getTime();
  const diffDays = Math.floor((target - startOfToday) / (1000 * 60 * 60 * 24));
  if (diffDays < 0) return 'overdue';
  if (diffDays <= 7) return 'soon';
  if (diffDays <= 30) return 'normal';
  return 'far';
}

/**
 * 並び替えキー: 未完了が上、期限超過 > 期限近い > 期限なし > 新着順、完了済は下。
 */
export function compareTodos(a: Todo, b: Todo): number {
  if (a.done !== b.done) return a.done ? 1 : -1;
  if (a.done && b.done) {
    return new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime();
  }
  const aHas = !!a.deadline;
  const bHas = !!b.deadline;
  if (aHas && bHas) {
    const at = new Date(a.deadline!).getTime();
    const bt = new Date(b.deadline!).getTime();
    if (at !== bt) return at - bt;
  } else if (aHas !== bHas) {
    return aHas ? -1 : 1;
  }
  return new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime();
}
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS — `[build] Complete!`

- [ ] **Step 3: Commit**

```bash
git add src/types/todo.ts
git commit -m "Add Todo type + city + urgency + sort helpers"
```

---

## Task 2: 初期空データファイル

**Files:**
- Create: `data/todos.json`

- [ ] **Step 1: 空配列で作成**

`data/todos.json` の内容:

```json
[]
```

(末尾改行 1 つ)

- [ ] **Step 2: Commit**

```bash
git add data/todos.json
git commit -m "Add empty todos.json fallback for SSR"
```

---

## Task 3: SSR Loader

**Files:**
- Create: `src/data/todo-loader.ts`

- [ ] **Step 1: ファイル作成**

`src/data/todo-loader.ts`:

```ts
import { createHash } from 'node:crypto';
import type { Todo, TodoCity } from '@/types/todo';
import { TODO_CITY_SET, compareTodos } from '@/types/todo';
import todosJson from '../../data/todos.json';

function generateId(title: string, createdAt: string): string {
  const hash = createHash('sha1').update(`${title}|${createdAt}`).digest('hex').slice(0, 8);
  return `todo-${hash}`;
}

function sanitize(item: unknown): Todo | null {
  if (!item || typeof item !== 'object') return null;
  const r = item as Record<string, unknown>;
  if (typeof r.title !== 'string' || !r.title.trim()) return null;

  const createdAt =
    typeof r.createdAt === 'string' && r.createdAt.trim()
      ? r.createdAt
      : new Date(0).toISOString();
  const updatedAt =
    typeof r.updatedAt === 'string' && r.updatedAt.trim()
      ? r.updatedAt
      : createdAt;

  const id =
    typeof r.id === 'string' && r.id.trim()
      ? r.id
      : generateId(r.title, createdAt);

  const city =
    typeof r.city === 'string' && TODO_CITY_SET.has(r.city as TodoCity)
      ? (r.city as TodoCity)
      : undefined;

  return {
    id,
    title: r.title.trim(),
    done: r.done === true,
    city,
    notes: typeof r.notes === 'string' ? r.notes : undefined,
    deadline: typeof r.deadline === 'string' && r.deadline.trim() ? r.deadline : undefined,
    createdAt,
    updatedAt,
    deletedAt: typeof r.deletedAt === 'string' && r.deletedAt.trim() ? r.deletedAt : undefined,
  };
}

export async function getTodos(): Promise<Todo[]> {
  const raw = todosJson as unknown[];
  const items: Todo[] = [];
  for (const r of raw) {
    const s = sanitize(r);
    if (s && !s.deletedAt) items.push(s);
  }
  return items.sort(compareTodos);
}
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/data/todo-loader.ts
git commit -m "Add Todo SSR loader with sanitize + tombstone filter"
```

---

## Task 4: 純関数 merge + テストスクリプト

**Files:**
- Create: `src/lib/todos-merge.ts`
- Create: `scripts/test-todos-merge.mjs`

- [ ] **Step 1: merge 関数作成**

`src/lib/todos-merge.ts`:

```ts
import type { Todo } from '../types/todo.js';

export interface MergeTodosResult {
  /** マージ結果の全アイテム (tombstone 含む) */
  next: Todo[];
  /** 反映された書き込みの id */
  applied: string[];
  /** 古いタイムスタンプで skip された書き込みの id と理由 */
  skipped: { id: string; reason: string }[];
}

/**
 * サーバー側の現在 todos 配列 (tombstone 含む) と、
 * クライアントから来た writes 配列をマージする。
 *
 * ルール: id ごとに updatedAt が新しい方を採用 (last-write-wins)。
 * 新規 id は無条件に追加。
 *
 * 純関数: 入力を mutate しない。
 *
 * 同タイムスタンプ (updatedAt が一致) は stale 扱い (strict `>` で比較)。
 * 同秒内の同時書き込みはサーバー優先。
 */
export function mergeTodos(server: Todo[], writes: Todo[]): MergeTodosResult {
  const byId = new Map<string, Todo>();
  for (const s of server) byId.set(s.id, s);

  const applied: string[] = [];
  const skipped: { id: string; reason: string }[] = [];

  for (const w of writes) {
    const existing = byId.get(w.id);
    if (!existing) {
      byId.set(w.id, w);
      applied.push(w.id);
      continue;
    }
    const wTime = new Date(w.updatedAt).getTime();
    const eTime = new Date(existing.updatedAt).getTime();
    if (!Number.isFinite(wTime) || !Number.isFinite(eTime)) {
      skipped.push({ id: w.id, reason: 'invalid-timestamp' });
      continue;
    }
    if (wTime > eTime) {
      byId.set(w.id, w);
      applied.push(w.id);
    } else {
      skipped.push({ id: w.id, reason: 'stale' });
    }
  }

  return { next: Array.from(byId.values()), applied, skipped };
}
```

- [ ] **Step 2: テストスクリプト作成**

`scripts/test-todos-merge.mjs`:

```js
// マージロジックの最小検証スクリプト
// 実行: node --experimental-strip-types scripts/test-todos-merge.mjs  (Node 22+)
//        または: pnpm exec tsx scripts/test-todos-merge.mjs
// 終了コード 0 = 全 PASS、非0 = FAIL

import { mergeTodos } from '../src/lib/todos-merge.ts';

let pass = 0, fail = 0;
function eq(actual, expected, label) {
  const a = JSON.stringify(actual);
  const e = JSON.stringify(expected);
  if (a === e) { pass++; console.log('  PASS:', label); }
  else { fail++; console.log('  FAIL:', label, '\n    actual  :', a, '\n    expected:', e); }
}

function makeTodo(id, title, updatedAt, opts = {}) {
  return {
    id, title, done: opts.done ?? false,
    createdAt: opts.createdAt ?? updatedAt,
    updatedAt,
    ...(opts.deletedAt ? { deletedAt: opts.deletedAt } : {}),
    ...(opts.city ? { city: opts.city } : {}),
  };
}

console.log('Test 1: new id is added');
{
  const server = [];
  const writes = [makeTodo('a', 'task A', '2026-06-23T10:00:00Z')];
  const r = mergeTodos(server, writes);
  eq(r.applied, ['a'], 'applied=[a]');
  eq(r.skipped, [], 'skipped=[]');
  eq(r.next.length, 1, 'next.length=1');
}

console.log('Test 2: newer overwrites older');
{
  const server = [makeTodo('a', 'OLD', '2026-06-23T09:00:00Z')];
  const writes = [makeTodo('a', 'NEW', '2026-06-23T10:00:00Z')];
  const r = mergeTodos(server, writes);
  eq(r.applied, ['a'], 'applied=[a]');
  eq(r.next[0].title, 'NEW', 'title=NEW');
}

console.log('Test 3: older is skipped (stale)');
{
  const server = [makeTodo('a', 'NEW', '2026-06-23T10:00:00Z')];
  const writes = [makeTodo('a', 'OLD', '2026-06-23T09:00:00Z')];
  const r = mergeTodos(server, writes);
  eq(r.applied, [], 'applied=[]');
  eq(r.skipped, [{ id: 'a', reason: 'stale' }], 'skipped=[{a,stale}]');
  eq(r.next[0].title, 'NEW', 'server preserved');
}

console.log('Test 4: tombstone wins over older edit');
{
  const server = [makeTodo('a', 'A', '2026-06-23T09:00:00Z')];
  const writes = [makeTodo('a', 'A', '2026-06-23T10:00:00Z', { deletedAt: '2026-06-23T10:00:00Z' })];
  const r = mergeTodos(server, writes);
  eq(r.applied, ['a'], 'applied=[a] (tombstone)');
  eq(r.next[0].deletedAt, '2026-06-23T10:00:00Z', 'deletedAt set');
}

console.log('Test 5: invalid timestamp is skipped');
{
  const server = [makeTodo('a', 'A', '2026-06-23T10:00:00Z')];
  const writes = [makeTodo('a', 'X', 'not-a-date')];
  const r = mergeTodos(server, writes);
  eq(r.skipped, [{ id: 'a', reason: 'invalid-timestamp' }], 'skipped=invalid-timestamp');
}

console.log('Test 6: mixed batch');
{
  const server = [
    makeTodo('a', 'A', '2026-06-23T09:00:00Z'),
    makeTodo('b', 'B', '2026-06-23T11:00:00Z'),
  ];
  const writes = [
    makeTodo('a', 'A2', '2026-06-23T10:00:00Z'),
    makeTodo('b', 'B2', '2026-06-23T10:00:00Z'),
    makeTodo('c', 'C', '2026-06-23T12:00:00Z'),
  ];
  const r = mergeTodos(server, writes);
  eq(r.applied.sort(), ['a', 'c'], 'applied=[a,c]');
  eq(r.skipped, [{ id: 'b', reason: 'stale' }], 'skipped=[b,stale]');
  eq(r.next.length, 3, 'next.length=3');
}

console.log('Test 7: equal timestamp is stale (server wins)');
{
  const ts = '2026-06-23T10:00:00Z';
  const server = [makeTodo('a', 'SERVER', ts)];
  const writes = [makeTodo('a', 'CLIENT', ts)];
  const r = mergeTodos(server, writes);
  eq(r.applied, [], 'applied=[]');
  eq(r.skipped, [{ id: 'a', reason: 'stale' }], 'skipped=[{a,stale}]');
  eq(r.next[0].title, 'SERVER', 'server value preserved');
}

console.log(`\n=== ${pass} passed, ${fail} failed ===`);
process.exit(fail > 0 ? 1 : 0);
```

- [ ] **Step 3: テスト実行**

Run: `node --experimental-strip-types scripts/test-todos-merge.mjs`
Expected: `=== 17 passed, 0 failed ===` (7 シナリオ、17 assertion)

代替: `pnpm exec tsx scripts/test-todos-merge.mjs`

- [ ] **Step 4: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/lib/todos-merge.ts scripts/test-todos-merge.mjs
git commit -m "Add todos merge logic (last-write-wins) with test script"
```

---

## Task 5: github-gist.ts 拡張

**Files:**
- Modify: `src/lib/github-gist.ts`

- [ ] **Step 1: 現在のファイル確認**

Run: `Read src/lib/github-gist.ts`
期待: `fetchItineraryGist` / `updateItineraryGist` / `GistContent` の構造を確認

- [ ] **Step 2: Todo 用関数を追加**

ファイル末尾に追加 (既存の `updateItineraryGist` の後ろ):

```ts
import type { Todo } from '../types/todo.js';

const TODOS_FILE_NAME = 'todos.json';

export interface TodosGistContent {
  version: 1;
  todos: Todo[];
  lastModifiedAt: string;
}

/**
 * Private Gist から todos データを取得。
 * `todos.json` がまだ存在しない (初回) ケースでは空の構造を返す。
 */
export async function fetchTodosGist(): Promise<TodosGistContent> {
  const gh = client();
  const res = await gh.gists.get({ gist_id: gistId() });
  const file = res.data.files?.[TODOS_FILE_NAME];
  if (!file || !file.content) {
    return { version: 1, todos: [], lastModifiedAt: new Date().toISOString() };
  }
  try {
    const parsed = JSON.parse(file.content);
    if (parsed && parsed.version === 1 && Array.isArray(parsed.todos)) {
      return parsed as TodosGistContent;
    }
  } catch {
    // fall through
  }
  return { version: 1, todos: [], lastModifiedAt: new Date().toISOString() };
}

/**
 * Private Gist の todos データを更新。`itinerary.json` には触らない。
 */
export async function updateTodosGist(todos: Todo[]): Promise<TodosGistContent> {
  const gh = client();
  const content: TodosGistContent = {
    version: 1,
    todos,
    lastModifiedAt: new Date().toISOString(),
  };
  await gh.gists.update({
    gist_id: gistId(),
    files: {
      [TODOS_FILE_NAME]: { content: JSON.stringify(content, null, 2) },
    },
  });
  return content;
}
```

注: `import type { Todo }` は既存の `import type { ItineraryItem }` のすぐ下に置く (ファイル上部にまとめる) のが整理上望ましい。

- [ ] **Step 3: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/lib/github-gist.ts
git commit -m "Extend github-gist with todos fetch/update (same gist, todos.json)"
```

---

## Task 6: API エンドポイント `/api/sync-todos`

**Files:**
- Create: `api/sync-todos.ts`

- [ ] **Step 1: エンドポイント作成**

`api/sync-todos.ts`:

```ts
import type { VercelRequest, VercelResponse } from '@vercel/node';
import { fetchTodosGist, updateTodosGist } from '../src/lib/github-gist.js';
import { mergeTodos } from '../src/lib/todos-merge.js';
import type { Todo, TodoCity } from '../src/types/todo.js';
import { TODO_CITY_SET } from '../src/types/todo.js';

function validateWrite(input: unknown): Todo | string {
  if (!input || typeof input !== 'object') return 'item must be object';
  const r = input as Record<string, unknown>;
  if (typeof r.id !== 'string' || !r.id.trim()) return 'id required';
  if (typeof r.updatedAt !== 'string' || !r.updatedAt.trim()) return 'updatedAt required';

  // tombstone: title 省略可
  if (typeof r.deletedAt === 'string' && r.deletedAt.trim()) {
    return {
      id: r.id.trim(),
      title: typeof r.title === 'string' ? r.title : '',
      done: r.done === true,
      city:
        typeof r.city === 'string' && TODO_CITY_SET.has(r.city as TodoCity)
          ? (r.city as TodoCity)
          : undefined,
      createdAt: typeof r.createdAt === 'string' ? r.createdAt : r.updatedAt,
      updatedAt: r.updatedAt,
      deletedAt: r.deletedAt,
    };
  }

  if (typeof r.title !== 'string' || !r.title.trim()) return 'title required';
  if (typeof r.createdAt !== 'string' || !r.createdAt.trim()) return 'createdAt required';

  const todo: Todo = {
    id: r.id.trim(),
    title: r.title.trim(),
    done: r.done === true,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt,
  };
  if (typeof r.city === 'string' && TODO_CITY_SET.has(r.city as TodoCity)) {
    todo.city = r.city as TodoCity;
  }
  if (typeof r.notes === 'string' && r.notes.trim()) todo.notes = r.notes.trim();
  if (typeof r.deadline === 'string' && r.deadline.trim()) todo.deadline = r.deadline;
  return todo;
}

function authOk(req: VercelRequest): string | null {
  const expected = process.env.ADMIN_PASSWORD;
  if (!expected) return 'ADMIN_PASSWORD not configured on server';
  const given = (req.headers['x-admin-password'] ?? '') as string;
  if (!given) return 'Missing X-Admin-Password header';
  if (given !== expected) return 'Invalid password';
  return null;
}

function checkGistConfig(res: VercelResponse): boolean {
  if (!process.env.ITINERARY_GIST_ID) {
    res.status(503).json({
      error: 'ITINERARY_GIST_ID not configured. Run: node scripts/setup-itinerary-gist.mjs',
    });
    return false;
  }
  if (!process.env.GITHUB_TOKEN) {
    res.status(503).json({ error: 'GITHUB_TOKEN not configured on server' });
    return false;
  }
  return true;
}

export default async function handler(req: VercelRequest, res: VercelResponse) {
  const authErr = authOk(req);
  if (authErr) return res.status(401).json({ error: authErr });
  if (!checkGistConfig(res)) return;

  try {
    if (req.method === 'GET') {
      const content = await fetchTodosGist();
      return res.status(200).json({
        version: content.version,
        todos: content.todos,
        serverTimestamp: new Date().toISOString(),
      });
    }

    if (req.method === 'POST') {
      const body = req.body as { writes?: unknown } | undefined;
      const raw = body?.writes;
      if (!Array.isArray(raw)) {
        return res.status(400).json({ error: 'writes must be an array' });
      }
      const writes: Todo[] = [];
      const rejected: { index: number; reason: string }[] = [];
      raw.forEach((w, i) => {
        const v = validateWrite(w);
        if (typeof v === 'string') rejected.push({ index: i, reason: v });
        else writes.push(v);
      });

      const current = await fetchTodosGist();
      const { next, applied, skipped } = mergeTodos(current.todos, writes);
      const updated = await updateTodosGist(next);

      return res.status(200).json({
        version: updated.version,
        todos: updated.todos,
        applied,
        skipped,
        rejected,
        serverTimestamp: new Date().toISOString(),
      });
    }

    return res.status(405).json({ error: 'Method not allowed' });
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Unknown error';
    return res.status(500).json({ error: msg });
  }
}
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add api/sync-todos.ts
git commit -m "Add /api/sync-todos endpoint (GET + POST with merge)"
```

---

## Task 7: TodoForm 編集モーダル

**Files:**
- Create: `src/components/TodoForm.astro`

- [ ] **Step 1: モーダル作成**

`src/components/TodoForm.astro`:

```astro
---
import { TODO_CITIES } from '@/types/todo';
---

<div id="todo-form-modal" class="todo-modal" hidden role="dialog" aria-label="ToDo 編集">
  <div class="todo-modal-backdrop" data-todo-close></div>

  <div class="todo-modal-panel" role="document">
    <header class="todo-modal-head">
      <h2 id="todo-form-title">ToDo を編集</h2>
      <button type="button" class="todo-modal-close" data-todo-close aria-label="閉じる">×</button>
    </header>

    <div id="todo-auth" class="todo-auth">
      <p>編集・削除には管理パスワードが必要です。</p>
      <input type="password" id="todo-password" placeholder="パスワード" autocomplete="current-password" />
      <button type="button" id="todo-auth-submit">ロック解除</button>
      <p class="todo-auth-error" id="todo-auth-error" hidden></p>
    </div>

    <form id="todo-form" class="todo-form" hidden>
      <input type="hidden" id="tf-id" />

      <label class="tf-row">
        <span>タイトル</span>
        <input type="text" id="tf-title" required maxlength="120" />
      </label>

      <fieldset class="tf-cities" aria-label="都市">
        <legend>都市</legend>
        {TODO_CITIES.map((c, i) => (
          <label class="tf-city-chip">
            <input type="radio" name="tf-city" value={c.value} checked={i === TODO_CITIES.length - 1} data-tf-city-input />
            <span>{c.label}</span>
          </label>
        ))}
      </fieldset>

      <label class="tf-row">
        <span>期限 (任意)</span>
        <input type="date" id="tf-deadline" />
      </label>

      <label class="tf-row">
        <span>メモ (任意)</span>
        <textarea id="tf-notes" rows="3" placeholder="補足、予約 URL、参考リンク等"></textarea>
      </label>

      <div class="tf-actions">
        <button type="button" class="tf-cancel" data-todo-close>キャンセル</button>
        <button type="submit" class="tf-submit">保存</button>
      </div>
      <p class="tf-status" id="tf-status" hidden></p>
    </form>
  </div>
</div>
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/components/TodoForm.astro
git commit -m "Add TodoForm modal component"
```

---

## Task 8: TodoList SSR コンポーネント

**Files:**
- Create: `src/components/TodoList.astro`

- [ ] **Step 1: SSR コンポーネント作成**

`src/components/TodoList.astro`:

```astro
---
import { TODO_CITIES, todoCityLabel, deadlineUrgency } from '@/types/todo';
import type { Todo, TodoCity } from '@/types/todo';

interface Props {
  todos: Todo[];
}
const { todos } = Astro.props;

const orderedCities: ReadonlyArray<TodoCity> = [
  'Rome', 'Florence', 'Palermo', 'Taormina', 'Sicily', '全般',
];

const groups: { city: TodoCity; items: Todo[] }[] = [];
for (const city of orderedCities) {
  const items = todos.filter((t) => (t.city ?? '全般') === city);
  if (items.length > 0) groups.push({ city, items });
}

function deadlineBadge(t: Todo): { text: string; urgency: string } {
  const u = deadlineUrgency(t.deadline);
  if (u === 'none' || !t.deadline) return { text: '', urgency: 'none' };
  const target = new Date(t.deadline);
  const now = new Date();
  const startOfToday = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
  const targetT = new Date(target.getFullYear(), target.getMonth(), target.getDate()).getTime();
  const diff = Math.floor((targetT - startOfToday) / (1000 * 60 * 60 * 24));
  if (u === 'overdue') return { text: `⚠ ${Math.abs(diff)}日超過`, urgency: 'overdue' };
  if (u === 'soon') return { text: `⏰ あと${diff}日`, urgency: 'soon' };
  const m = String(target.getMonth() + 1);
  const d = String(target.getDate());
  return { text: `📅 ${m}月${d}日`, urgency: u };
}
---

<section class="todo-list" data-todo-root>
  {todos.length === 0 && (
    <div class="todo-empty">
      <p>まだ ToDo がありません。</p>
      <p>上の入力欄から追加できます。</p>
    </div>
  )}

  {groups.map((g) => (
    <div class="todo-city-group" data-city={g.city}>
      <h2 class="todo-city-head">{todoCityLabel(g.city)} <span class="todo-city-count">({g.items.length})</span></h2>
      <ul class="todo-items">
        {g.items.map((t) => {
          const badge = deadlineBadge(t);
          return (
            <li class={`todo-card${t.done ? ' is-done' : ''}`} data-id={t.id} data-city={t.city ?? '全般'}>
              <label class="todo-check">
                <input type="checkbox" data-todo-check={t.id} checked={t.done} />
                <span class="todo-check-box" aria-hidden="true"></span>
              </label>
              <div class="todo-body">
                <p class="todo-title">{t.title}</p>
                {t.notes && <p class="todo-notes">{t.notes}</p>}
              </div>
              {badge.text && (
                <span class="todo-deadline-badge" data-urgency={badge.urgency}>{badge.text}</span>
              )}
              <div class="todo-actions">
                <button type="button" class="todo-edit" data-edit-id={t.id} aria-label="編集">✎</button>
                <button type="button" class="todo-delete" data-delete-id={t.id} aria-label="削除">🗑</button>
              </div>
            </li>
          );
        })}
      </ul>
    </div>
  ))}
</section>
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/components/TodoList.astro
git commit -m "Add TodoList SSR component (city grouping + deadline badges)"
```

---

## Task 9: ToDo ページ + クライアント sync ロジック

**Files:**
- Create: `src/pages/todo.astro`

- [ ] **Step 1: ページ作成**

`src/pages/todo.astro`:

```astro
---
import Layout from '@/layouts/Layout.astro';
import TodoList from '@/components/TodoList.astro';
import TodoForm from '@/components/TodoForm.astro';
import { TODO_CITIES } from '@/types/todo';
import { getTodos } from '@/data/todo-loader';

const todos = await getTodos();
---

<Layout title="ToDo - イタリアご飯マップ">
  <header class="app-header">
    <h1>ToDo</h1>
    <nav class="app-nav">
      <a href="/" class="app-nav-link">🗺 マップ</a>
      <a href="/itinerary" class="app-nav-link">📅 旅程</a>
      <button type="button" id="todo-sync-btn" class="app-nav-link" title="同期" aria-label="今すぐ同期">🔄</button>
      <span id="todo-sync-status" class="itinerary-sync-status" data-state="idle" aria-live="polite" aria-atomic="true">—</span>
    </nav>
    <span class="card-meta" id="todo-count">{todos.length} 件</span>
  </header>

  <main class="todo-main">
    <div class="todo-quick-add">
      <input type="text" id="quick-add-title" placeholder="新しい ToDo を入力 → Enter" maxlength="120" />
      <select id="quick-add-city" aria-label="都市">
        {TODO_CITIES.map((c, i) => (
          <option value={c.value} selected={i === TODO_CITIES.length - 1}>{c.label}</option>
        ))}
      </select>
      <button type="button" id="quick-add-btn">追加</button>
    </div>

    <div class="todo-filter-bar">
      <div class="todo-city-chips" role="group" aria-label="都市フィルタ">
        <button type="button" class="todo-chip is-active" data-filter-city="">すべて</button>
        {TODO_CITIES.map((c) => (
          <button type="button" class="todo-chip" data-filter-city={c.value}>{c.label}</button>
        ))}
      </div>
      <label class="todo-hide-done">
        <input type="checkbox" id="hide-done-toggle" checked />
        <span>未完了のみ</span>
      </label>
    </div>

    <TodoList todos={todos} />
  </main>

  <TodoForm />

  <script type="application/json" id="todo-data" is:inline set:html={JSON.stringify(todos)} />
</Layout>

<script>
  type TodoCity = 'Rome' | 'Florence' | 'Palermo' | 'Taormina' | 'Sicily' | '全般';
  type Todo = {
    id: string;
    title: string;
    done: boolean;
    city?: TodoCity;
    notes?: string;
    deadline?: string;
    createdAt: string;
    updatedAt: string;
    deletedAt?: string;
  };

  const PASSWORD_KEY = 'italia-gohan-chat-password';
  const CACHE_KEY = 'italia-gohan-todo-cache';
  const QUEUE_KEY = 'italia-gohan-todo-queue';
  const LAST_SYNC_KEY = 'italia-gohan-todo-last-sync';

  let draining = false;

  const getPassword = () => sessionStorage.getItem(PASSWORD_KEY);
  const setPassword = (p: string) => sessionStorage.setItem(PASSWORD_KEY, p);
  const clearPassword = () => sessionStorage.removeItem(PASSWORD_KEY);

  function loadCache(): Todo[] {
    try {
      const raw = localStorage.getItem(CACHE_KEY);
      if (!raw) return [];
      const p = JSON.parse(raw);
      return Array.isArray(p) ? (p as Todo[]) : [];
    } catch { return []; }
  }
  function saveCache(items: Todo[]) {
    localStorage.setItem(CACHE_KEY, JSON.stringify(items));
  }
  function upsertCache(item: Todo) {
    const all = loadCache();
    const idx = all.findIndex((x) => x.id === item.id);
    if (idx >= 0) all[idx] = item; else all.push(item);
    saveCache(all);
  }
  function deleteCache(id: string) {
    saveCache(loadCache().filter((x) => x.id !== id));
  }
  function findCache(id: string): Todo | undefined {
    return loadCache().find((x) => x.id === id);
  }

  function loadQueue(): Record<string, Todo> {
    try {
      const raw = localStorage.getItem(QUEUE_KEY);
      if (!raw) return {};
      const p = JSON.parse(raw);
      return typeof p === 'object' && p ? (p as Record<string, Todo>) : {};
    } catch { return {}; }
  }
  function saveQueue(q: Record<string, Todo>) {
    localStorage.setItem(QUEUE_KEY, JSON.stringify(q));
  }
  function enqueueWrite(item: Todo) {
    const q = loadQueue();
    q[item.id] = item;
    saveQueue(q);
  }
  function dequeueWrite(id: string) {
    const q = loadQueue();
    delete q[id];
    saveQueue(q);
  }
  function getLastSync(): string | null {
    return localStorage.getItem(LAST_SYNC_KEY);
  }
  function setLastSync(ts: string) {
    localStorage.setItem(LAST_SYNC_KEY, ts);
  }

  function inlineSeed(): Todo[] {
    const el = document.getElementById('todo-data');
    if (!el || !el.textContent) return [];
    try { return JSON.parse(el.textContent); } catch { return []; }
  }

  function mergeInitialCache() {
    const cache = loadCache();
    if (cache.length === 0) {
      const seed = inlineSeed();
      if (seed.length > 0) saveCache(seed);
    }
  }

  function escapeHtml(s: string): string {
    return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
  }

  function deadlineUrgency(deadline: string | undefined, today: Date = new Date()): 'overdue' | 'soon' | 'normal' | 'far' | 'none' {
    if (!deadline) return 'none';
    const d = new Date(deadline);
    if (!Number.isFinite(d.getTime())) return 'none';
    const startOfToday = new Date(today.getFullYear(), today.getMonth(), today.getDate()).getTime();
    const target = new Date(d.getFullYear(), d.getMonth(), d.getDate()).getTime();
    const diff = Math.floor((target - startOfToday) / (1000 * 60 * 60 * 24));
    if (diff < 0) return 'overdue';
    if (diff <= 7) return 'soon';
    if (diff <= 30) return 'normal';
    return 'far';
  }

  function deadlineBadgeText(t: Todo): { text: string; urgency: string } {
    const u = deadlineUrgency(t.deadline);
    if (u === 'none' || !t.deadline) return { text: '', urgency: 'none' };
    const target = new Date(t.deadline);
    const now = new Date();
    const startOfToday = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
    const targetT = new Date(target.getFullYear(), target.getMonth(), target.getDate()).getTime();
    const diff = Math.floor((targetT - startOfToday) / (1000 * 60 * 60 * 24));
    if (u === 'overdue') return { text: `⚠ ${Math.abs(diff)}日超過`, urgency: 'overdue' };
    if (u === 'soon') return { text: `⏰ あと${diff}日`, urgency: 'soon' };
    const m = String(target.getMonth() + 1);
    const d = String(target.getDate());
    return { text: `📅 ${m}月${d}日`, urgency: u };
  }

  function compareTodos(a: Todo, b: Todo): number {
    if (a.done !== b.done) return a.done ? 1 : -1;
    if (a.done && b.done) return new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime();
    const aHas = !!a.deadline; const bHas = !!b.deadline;
    if (aHas && bHas) {
      const at = new Date(a.deadline!).getTime();
      const bt = new Date(b.deadline!).getTime();
      if (at !== bt) return at - bt;
    } else if (aHas !== bHas) return aHas ? -1 : 1;
    return new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime();
  }

  const CITY_LABEL: Record<string, string> = {
    Rome: 'ローマ', Florence: 'フィレンツェ', Palermo: 'パレルモ',
    Taormina: 'タオルミーナ', Sicily: 'シチリア', '全般': '全般',
  };
  const CITY_ORDER: TodoCity[] = ['Rome', 'Florence', 'Palermo', 'Taormina', 'Sicily', '全般'];

  function renderAll() {
    const root = document.querySelector('.todo-list');
    if (!root) return;
    const all = loadCache().filter((x) => !x.deletedAt);
    const filterCity = (document.querySelector('.todo-chip.is-active') as HTMLElement | null)?.getAttribute('data-filter-city') ?? '';
    const hideDone = (document.getElementById('hide-done-toggle') as HTMLInputElement | null)?.checked ?? true;
    const filtered = all.filter((t) => {
      if (filterCity && (t.city ?? '全般') !== filterCity) return false;
      if (hideDone && t.done) return false;
      return true;
    }).sort(compareTodos);

    if (filtered.length === 0) {
      root.innerHTML = `<div class="todo-empty"><p>該当する ToDo がありません。</p></div>`;
      updateCount();
      return;
    }

    const groups: { city: TodoCity; items: Todo[] }[] = [];
    for (const city of CITY_ORDER) {
      const items = filtered.filter((t) => (t.city ?? '全般') === city);
      if (items.length > 0) groups.push({ city, items });
    }

    root.innerHTML = groups.map((g) => `
      <div class="todo-city-group" data-city="${escapeHtml(g.city)}">
        <h2 class="todo-city-head">${escapeHtml(CITY_LABEL[g.city] ?? g.city)} <span class="todo-city-count">(${g.items.length})</span></h2>
        <ul class="todo-items">
          ${g.items.map((t) => {
            const b = deadlineBadgeText(t);
            return `
              <li class="todo-card${t.done ? ' is-done' : ''}" data-id="${escapeHtml(t.id)}" data-city="${escapeHtml(t.city ?? '全般')}">
                <label class="todo-check">
                  <input type="checkbox" data-todo-check="${escapeHtml(t.id)}" ${t.done ? 'checked' : ''} />
                  <span class="todo-check-box" aria-hidden="true"></span>
                </label>
                <div class="todo-body">
                  <p class="todo-title">${escapeHtml(t.title)}</p>
                  ${t.notes ? `<p class="todo-notes">${escapeHtml(t.notes)}</p>` : ''}
                </div>
                ${b.text ? `<span class="todo-deadline-badge" data-urgency="${escapeHtml(b.urgency)}">${escapeHtml(b.text)}</span>` : ''}
                <div class="todo-actions">
                  <button type="button" class="todo-edit" data-edit-id="${escapeHtml(t.id)}" aria-label="編集">✎</button>
                  <button type="button" class="todo-delete" data-delete-id="${escapeHtml(t.id)}" aria-label="削除">🗑</button>
                </div>
              </li>`;
          }).join('')}
        </ul>
      </div>`).join('');

    attachCardListeners();
    updateCount();
  }

  function updateCount() {
    const el = document.getElementById('todo-count');
    if (!el) return;
    const total = loadCache().filter((x) => !x.deletedAt).length;
    el.textContent = `${total} 件`;
  }

  function updateSyncBadge() {
    const el = document.getElementById('todo-sync-status');
    if (!el) return;
    const queueSize = Object.keys(loadQueue()).length;
    const ls = getLastSync();
    if (!navigator.onLine) {
      el.dataset.state = 'offline';
      el.textContent = queueSize > 0 ? `📴 オフライン (${queueSize} 件保留)` : '📴 オフライン';
      return;
    }
    if (queueSize > 0) {
      el.dataset.state = 'syncing';
      el.textContent = `🔄 同期中 (${queueSize})`;
      return;
    }
    if (ls) {
      const d = new Date(ls);
      const hm = d.toLocaleTimeString('ja-JP', { hour: '2-digit', minute: '2-digit' });
      el.dataset.state = 'ok';
      el.textContent = `✓ 同期済み ${hm}`;
    } else {
      el.dataset.state = 'idle';
      el.textContent = '—';
    }
  }

  async function fetchLatestFromServer(): Promise<Todo[] | null> {
    const password = getPassword();
    if (!password || !navigator.onLine) return null;
    try {
      const res = await fetch('/api/sync-todos', {
        method: 'GET',
        headers: { 'x-admin-password': password },
      });
      if (res.status === 401) { clearPassword(); return null; }
      if (!res.ok) return null;
      const json = await res.json();
      if (!Array.isArray(json.todos)) return null;
      setLastSync(json.serverTimestamp ?? new Date().toISOString());
      return json.todos as Todo[];
    } catch { return null; }
  }

  async function drainQueue() {
    if (draining) return;
    const password = getPassword();
    if (!password || !navigator.onLine) return;
    const snapshot = loadQueue();
    const writes = Object.values(snapshot);
    if (writes.length === 0) return;
    draining = true;
    try {
      const res = await fetch('/api/sync-todos', {
        method: 'POST',
        headers: { 'content-type': 'application/json', 'x-admin-password': password },
        body: JSON.stringify({ writes }),
      });
      if (!res.ok) { console.warn('[sync-todos] drain failed', res.status); return; }
      const json = await res.json();
      const safeDequeue = (id: string) => {
        const current = loadQueue()[id];
        const sent = snapshot[id];
        if (current && sent && current.updatedAt === sent.updatedAt) dequeueWrite(id);
      };
      (json.applied ?? []).forEach((id: string) => safeDequeue(id));
      (json.skipped ?? []).forEach((s: { id: string }) => safeDequeue(s.id));
      (json.rejected ?? []).forEach((r: { index: number; reason: string }) => {
        const sent = writes[r.index];
        if (sent) {
          console.warn('[sync-todos] write rejected', sent.id, r.reason);
          safeDequeue(sent.id);
        }
      });
      if (Array.isArray(json.todos)) applyServerTodos(json.todos as Todo[]);
      setLastSync(json.serverTimestamp ?? new Date().toISOString());
    } catch (e) {
      console.warn('[sync-todos] network error', e);
    } finally {
      draining = false;
      updateSyncBadge();
    }
  }

  function applyServerTodos(serverItems: Todo[]) {
    const queue = loadQueue();
    const next = serverItems.filter((x) => !x.deletedAt);
    for (const [id, qItem] of Object.entries(queue)) {
      const sIdx = next.findIndex((x) => x.id === id);
      const sItem = sIdx >= 0 ? next[sIdx] : null;
      const qTime = qItem.updatedAt ? new Date(qItem.updatedAt).getTime() : 0;
      const sTime = sItem?.updatedAt ? new Date(sItem.updatedAt).getTime() : 0;
      if (qTime > sTime && !qItem.deletedAt) {
        if (sIdx >= 0) next[sIdx] = qItem; else next.push(qItem);
      }
    }
    saveCache(next);
    renderAll();
  }

  function cryptoRandomShort(): string {
    const arr = new Uint8Array(4);
    crypto.getRandomValues(arr);
    return Array.from(arr).map((b) => b.toString(16).padStart(2, '0')).join('');
  }

  function quickAdd() {
    const titleEl = document.getElementById('quick-add-title') as HTMLInputElement;
    const cityEl = document.getElementById('quick-add-city') as HTMLSelectElement;
    const title = titleEl.value.trim();
    if (!title) return;
    const now = new Date().toISOString();
    const item: Todo = {
      id: `todo-${cryptoRandomShort()}`,
      title,
      done: false,
      city: cityEl.value as TodoCity,
      createdAt: now,
      updatedAt: now,
    };
    upsertCache(item);
    enqueueWrite(item);
    titleEl.value = '';
    titleEl.focus();
    renderAll();
    updateSyncBadge();
    void drainQueue();
  }

  function toggleDone(id: string) {
    const t = findCache(id);
    if (!t) return;
    const updated: Todo = { ...t, done: !t.done, updatedAt: new Date().toISOString() };
    upsertCache(updated);
    enqueueWrite(updated);
    renderAll();
    updateSyncBadge();
    void drainQueue();
  }

  function attachCardListeners() {
    document.querySelectorAll<HTMLInputElement>('[data-todo-check]').forEach((cb) => {
      if (cb.dataset.bound === '1') return;
      cb.dataset.bound = '1';
      cb.addEventListener('change', () => {
        const id = cb.getAttribute('data-todo-check');
        if (id) toggleDone(id);
      });
    });
    document.querySelectorAll<HTMLButtonElement>('[data-edit-id]').forEach((btn) => {
      if (btn.dataset.bound === '1') return;
      btn.dataset.bound = '1';
      btn.addEventListener('click', () => {
        const id = btn.getAttribute('data-edit-id');
        if (id) openEditModal(id);
      });
    });
    document.querySelectorAll<HTMLButtonElement>('[data-delete-id]').forEach((btn) => {
      if (btn.dataset.bound === '1') return;
      btn.dataset.bound = '1';
      btn.addEventListener('click', () => {
        const id = btn.getAttribute('data-delete-id');
        if (!id) return;
        if (!confirm('この ToDo を削除しますか？')) return;
        const t = findCache(id);
        if (!t) return;
        const tomb: Todo = { ...t, deletedAt: new Date().toISOString(), updatedAt: new Date().toISOString() };
        enqueueWrite(tomb);
        deleteCache(id);
        renderAll();
        updateSyncBadge();
        void drainQueue();
      });
    });
  }

  function openEditModal(id: string) {
    const t = findCache(id);
    const modal = document.getElementById('todo-form-modal');
    if (!modal) return;
    modal.hidden = false;
    const auth = document.getElementById('todo-auth') as HTMLElement;
    const form = document.getElementById('todo-form') as HTMLElement;
    if (getPassword()) { auth.hidden = true; form.hidden = false; }
    else { auth.hidden = false; form.hidden = true; }
    fillForm(t);
  }
  function closeModal() {
    const modal = document.getElementById('todo-form-modal');
    if (modal) modal.hidden = true;
  }

  function fillForm(t?: Todo) {
    (document.getElementById('tf-id') as HTMLInputElement).value = t?.id ?? '';
    (document.getElementById('tf-title') as HTMLInputElement).value = t?.title ?? '';
    (document.getElementById('tf-deadline') as HTMLInputElement).value = t?.deadline ?? '';
    (document.getElementById('tf-notes') as HTMLTextAreaElement).value = t?.notes ?? '';
    const city = t?.city ?? '全般';
    document.querySelectorAll<HTMLInputElement>('[data-tf-city-input]').forEach((r) => {
      r.checked = r.value === city;
    });
    const status = document.getElementById('tf-status');
    if (status) { status.hidden = true; status.textContent = ''; }
  }

  function gatherForm(): Todo | string {
    const id = (document.getElementById('tf-id') as HTMLInputElement).value.trim();
    if (!id) return 'id missing';
    const title = (document.getElementById('tf-title') as HTMLInputElement).value.trim();
    if (!title) return 'タイトルを入力してください';
    const deadline = (document.getElementById('tf-deadline') as HTMLInputElement).value;
    const notes = (document.getElementById('tf-notes') as HTMLTextAreaElement).value.trim();
    const cityInput = document.querySelector<HTMLInputElement>('[data-tf-city-input]:checked');
    const city = (cityInput?.value ?? '全般') as TodoCity;

    const existing = findCache(id);
    if (!existing) return '対象が見つかりません';
    const now = new Date().toISOString();
    return {
      ...existing,
      title,
      city,
      ...(deadline ? { deadline } : { deadline: undefined }),
      ...(notes ? { notes } : { notes: undefined }),
      updatedAt: now,
    };
  }

  function submitEdit() {
    const v = gatherForm();
    if (typeof v === 'string') {
      const status = document.getElementById('tf-status');
      if (status) { status.hidden = false; status.textContent = v; }
      return;
    }
    upsertCache(v);
    enqueueWrite(v);
    renderAll();
    updateSyncBadge();
    void drainQueue();
    closeModal();
  }

  // === filter chips ===
  document.querySelectorAll<HTMLButtonElement>('[data-filter-city]').forEach((btn) => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('[data-filter-city]').forEach((b) => b.classList.remove('is-active'));
      btn.classList.add('is-active');
      renderAll();
    });
  });
  document.getElementById('hide-done-toggle')?.addEventListener('change', () => renderAll());

  // === quick add ===
  const quickInput = document.getElementById('quick-add-title') as HTMLInputElement | null;
  quickInput?.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') { e.preventDefault(); quickAdd(); }
  });
  document.getElementById('quick-add-btn')?.addEventListener('click', () => quickAdd());

  // === modal ===
  document.querySelectorAll<HTMLElement>('[data-todo-close]').forEach((el) => {
    el.addEventListener('click', closeModal);
  });
  document.getElementById('todo-auth-submit')?.addEventListener('click', () => {
    const input = document.getElementById('todo-password') as HTMLInputElement;
    const v = input.value.trim();
    if (!v) return;
    setPassword(v);
    (document.getElementById('todo-auth') as HTMLElement).hidden = true;
    (document.getElementById('todo-form') as HTMLElement).hidden = false;
  });
  document.getElementById('todo-form')?.addEventListener('submit', (e) => {
    e.preventDefault();
    submitEdit();
  });

  // === sync button ===
  document.getElementById('todo-sync-btn')?.addEventListener('click', async (e) => {
    const btn = e.currentTarget as HTMLButtonElement;
    if (btn.disabled) return;
    btn.disabled = true;
    btn.setAttribute('aria-busy', 'true');
    const statusEl = document.getElementById('todo-sync-status');
    if (statusEl) { statusEl.dataset.state = 'syncing'; statusEl.textContent = '🔄 同期中…'; }
    try {
      const latest = await fetchLatestFromServer();
      if (latest) applyServerTodos(latest);
      await drainQueue();
    } catch (err) {
      console.warn('[sync-todos] manual sync failed', err);
    } finally {
      btn.disabled = false;
      btn.removeAttribute('aria-busy');
      updateSyncBadge();
    }
  });

  // === init ===
  mergeInitialCache();
  renderAll();

  void (async () => {
    const latest = await fetchLatestFromServer();
    if (latest) applyServerTodos(latest);
    await drainQueue();
    updateSyncBadge();
  })();

  window.addEventListener('online', () => { void drainQueue(); updateSyncBadge(); });
  window.addEventListener('offline', () => { updateSyncBadge(); });
  setInterval(updateSyncBadge, 30_000);

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      const modal = document.getElementById('todo-form-modal');
      if (modal && !modal.hidden) closeModal();
    }
  });
</script>
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS — `[build] Complete!`、3 page(s) built (`/`、`/itinerary`、`/todo`)

- [ ] **Step 3: Commit**

```bash
git add src/pages/todo.astro
git commit -m "Add /todo page with sync + quick-add + filter + edit"
```

---

## Task 10: CSS スタイル

**Files:**
- Modify: `src/styles/global.css`

- [ ] **Step 1: ファイル末尾に Todo セクションを追加**

`src/styles/global.css` の **末尾** (`.itinerary-priv-badge` / `.itinerary-sync-status` 関連の末尾) に追記:

```css

/* ============================================================ */
/* ToDo (Phase 8 / D-032) */
/* ============================================================ */

.todo-main {
  padding: 16px;
  max-width: 900px;
  margin: 0 auto;
}

.todo-quick-add {
  display: flex;
  gap: 8px;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 10px 12px;
  margin-bottom: 12px;
  box-shadow: var(--shadow-sm);
}
.todo-quick-add input[type="text"] {
  flex: 1;
  padding: 8px 10px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  font: inherit;
  font-size: 0.95rem;
}
.todo-quick-add select {
  padding: 8px 10px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  font-family: inherit;
  font-size: 0.9rem;
  background: var(--color-bg);
}
.todo-quick-add button {
  padding: 8px 14px;
  background: var(--color-accent);
  color: white;
  border: none;
  border-radius: var(--radius-sm);
  cursor: pointer;
  font-weight: 600;
}
.todo-quick-add button:hover {
  background: var(--color-accent-hover);
}

.todo-filter-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
}
.todo-city-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.todo-chip {
  padding: 6px 10px;
  border: 1px solid var(--color-border);
  border-radius: 999px;
  background: var(--color-surface-2);
  font-size: 0.85rem;
  cursor: pointer;
  color: var(--color-text);
}
.todo-chip:hover {
  background: var(--color-surface);
}
.todo-chip.is-active {
  background: var(--color-accent);
  color: white;
  border-color: var(--color-accent);
}
.todo-hide-done {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 0.85rem;
  color: var(--color-text-muted);
  cursor: pointer;
}

.todo-empty {
  background: var(--color-surface);
  border: 1px dashed var(--color-border);
  border-radius: var(--radius-md);
  padding: 32px 20px;
  text-align: center;
  color: var(--color-text-muted);
}
.todo-empty p {
  margin: 6px 0;
}

.todo-city-group {
  margin-bottom: 18px;
}
.todo-city-head {
  font-size: 1rem;
  font-weight: 600;
  color: var(--color-text-muted);
  margin: 0 0 8px 0;
  padding-bottom: 6px;
  border-bottom: 1px solid var(--color-border);
}
.todo-city-count {
  font-size: 0.8rem;
  color: var(--color-text-muted);
  margin-left: 6px;
}

.todo-items {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.todo-card {
  display: grid;
  grid-template-columns: auto 1fr auto auto;
  gap: 10px;
  align-items: center;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 10px 12px;
  box-shadow: var(--shadow-sm);
  transition: opacity 0.15s ease;
}
.todo-card.is-done {
  opacity: 0.55;
}
.todo-card.is-done .todo-title {
  text-decoration: line-through;
}

.todo-check {
  display: inline-flex;
  align-items: center;
  cursor: pointer;
  position: relative;
  padding: 4px;
}
.todo-check input {
  position: absolute;
  opacity: 0;
  pointer-events: none;
}
.todo-check-box {
  display: inline-block;
  width: 22px;
  height: 22px;
  border: 2px solid var(--color-border);
  border-radius: 6px;
  background: var(--color-surface);
  position: relative;
}
.todo-check input:checked + .todo-check-box {
  background: var(--color-accent);
  border-color: var(--color-accent);
}
.todo-check input:checked + .todo-check-box::after {
  content: '✓';
  color: white;
  position: absolute;
  top: -2px;
  left: 3px;
  font-size: 18px;
  font-weight: bold;
}

.todo-body {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}
.todo-title {
  margin: 0;
  font-size: 0.95rem;
  word-break: break-word;
}
.todo-notes {
  margin: 0;
  font-size: 0.8rem;
  color: var(--color-text-muted);
  word-break: break-word;
}

.todo-deadline-badge {
  font-size: 0.75rem;
  padding: 3px 8px;
  border-radius: 999px;
  white-space: nowrap;
  background: var(--color-surface-2);
  color: var(--color-text-muted);
  border: 1px solid var(--color-border);
}
.todo-deadline-badge[data-urgency="overdue"] {
  background: #fee2e2;
  color: #991b1b;
  border-color: #fca5a5;
  font-weight: 600;
}
.todo-deadline-badge[data-urgency="soon"] {
  background: #fef3c7;
  color: #92400e;
  border-color: #fcd34d;
}

.todo-actions {
  display: flex;
  gap: 4px;
}
.todo-edit,
.todo-delete {
  background: none;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: 4px 8px;
  cursor: pointer;
  font-size: 0.85rem;
  color: var(--color-text-muted);
}
.todo-edit:hover { background: var(--color-surface-2); }
.todo-delete:hover { background: #fee2e2; color: var(--color-unvisited); }

@media (max-width: 480px) {
  .todo-card {
    grid-template-columns: auto 1fr;
  }
  .todo-deadline-badge,
  .todo-actions {
    grid-column: 2;
    justify-self: start;
  }
  .todo-actions {
    margin-top: 4px;
  }
}

/* Todo Edit Modal */
.todo-modal {
  position: fixed;
  inset: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
}
.todo-modal-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
}
.todo-modal-panel {
  position: relative;
  background: var(--color-surface);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  width: min(520px, calc(100vw - 24px));
  max-height: calc(100vh - 24px);
  overflow-y: auto;
  padding: 16px;
}
.todo-modal-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}
.todo-modal-head h2 { margin: 0; font-size: 1.1rem; }
.todo-modal-close {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  color: var(--color-text-muted);
}

.todo-auth {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.todo-auth input {
  padding: 8px 10px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
}
.todo-auth button {
  background: var(--color-accent);
  color: white;
  border: none;
  border-radius: var(--radius-sm);
  padding: 8px 14px;
  cursor: pointer;
}
.todo-auth-error {
  color: var(--color-unvisited);
  font-size: 0.85rem;
  margin: 0;
}

.todo-form {
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.tf-row {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.tf-row > span {
  font-size: 0.8rem;
  color: var(--color-text-muted);
  font-weight: 500;
}
.tf-row input,
.tf-row textarea {
  padding: 7px 10px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  font: inherit;
  font-size: 0.95rem;
}
.tf-cities {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  border: none;
  padding: 0;
  margin: 0;
}
.tf-cities legend {
  font-size: 0.8rem;
  color: var(--color-text-muted);
  font-weight: 500;
  margin-bottom: 4px;
}
.tf-city-chip {
  display: inline-flex;
  cursor: pointer;
  position: relative;
}
.tf-city-chip input {
  position: absolute;
  opacity: 0;
}
.tf-city-chip span {
  display: inline-block;
  padding: 6px 10px;
  border: 1px solid var(--color-border);
  border-radius: 999px;
  font-size: 0.85rem;
  background: var(--color-surface-2);
}
.tf-city-chip input:checked + span {
  background: var(--color-accent);
  color: white;
  border-color: var(--color-accent);
}
.tf-actions {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
}
.tf-cancel, .tf-submit {
  padding: 8px 16px;
  border-radius: var(--radius-sm);
  cursor: pointer;
  border: 1px solid var(--color-border);
  font-size: 0.95rem;
}
.tf-cancel { background: var(--color-surface); color: var(--color-text); }
.tf-submit { background: var(--color-accent); color: white; border-color: var(--color-accent); }
.tf-submit:hover { background: var(--color-accent-hover); }
.tf-status {
  margin: 0;
  font-size: 0.85rem;
  color: var(--color-text-muted);
}
```

- [ ] **Step 2: ビルド検証**

Run: `pnpm build`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/styles/global.css
git commit -m "Add Todo CSS (quick-add, filter chips, cards, modal)"
```

---

## Task 11: ナビリンク + 決定ログ

**Files:**
- Modify: `src/pages/index.astro`
- Modify: `src/pages/itinerary.astro`
- Modify: `docs/plan/decisions-log.md`

- [ ] **Step 1: index.astro のヘッダーに `/todo` リンク追加**

`src/pages/index.astro` を Read。`<nav class="app-nav">` 内を以下に変更 (旅程リンクの後に追加):

```astro
<nav class="app-nav">
  <a href="/itinerary" class="app-nav-link">📅 旅程</a>
  <a href="/todo" class="app-nav-link">✓ ToDo</a>
</nav>
```

- [ ] **Step 2: itinerary.astro のヘッダーに `/todo` リンク追加**

`src/pages/itinerary.astro` を Read。`<nav class="app-nav">` 内の既存リンクの直後に挿入:

```astro
<nav class="app-nav">
  <a href="/" class="app-nav-link">🗺 マップに戻る</a>
  <a href="/todo" class="app-nav-link">✓ ToDo</a>
  <button type="button" id="itinerary-sync-btn" class="app-nav-link" title="同期" aria-label="今すぐ同期">🔄</button>
  <span id="itinerary-sync-status" class="itinerary-sync-status" data-state="idle" aria-live="polite" aria-atomic="true">—</span>
</nav>
```

- [ ] **Step 3: 決定ログに D-032 を追記**

`docs/plan/decisions-log.md` の末尾に追記:

```markdown

## D-032: Phase 8 — 旅行 ToDo リスト機能

- 日付: 2026-06-23
- 状態: Active
- 関連: [src/types/todo.ts](../../src/types/todo.ts), [src/pages/todo.astro](../../src/pages/todo.astro), [api/sync-todos.ts](../../../api/sync-todos.ts)
- 決定: 旅行前準備タスク (ホテル決定、コロッセオ予約、レストラン予約等) を管理する新ページ `/todo` を追加。D-031 (旅程 sync) の Private Gist インフラを再利用、同 Gist 内に `todos.json` を新設
- ユーザー確認事項 (2026-06-23):
  - 既存データとの連携: 完全に独立した task list (旅程/レストランへのリンクなし)
  - 保存先: 既存 Private Gist の別ファイル `todos.json`、新規 env var なし
  - UI: 新規ページ `/todo`、ヘッダーに ✓ ToDo リンク
  - 分類: 都市別グルーピング (Rome / Florence / Palermo / Taormina / Sicily / 全般) + フィルタチップ
- スキーマ要点:
  ```ts
  type TodoCity = 'Rome'|'Florence'|'Palermo'|'Taormina'|'Sicily'|'全般';
  interface Todo {
    id: string; title: string; done: boolean;
    city?: TodoCity; notes?: string; deadline?: string;
    createdAt: string; updatedAt: string; deletedAt?: string;
  }
  ```
- 機能:
  - クイック追加 (タイトル + 都市のみ、Enter で確定)
  - チェックボックスで done toggle、即同期
  - ✎ で編集モーダル (タイトル/都市/期限/メモ)
  - 🗑 削除 → tombstone 伝播
  - 期限視覚化: 超過 🔴 / 7日以内 🟠 / 通常 / 期限なし
  - 並び替え: 未完了 → 完了の順、未完了内は期限超過 → 期限近い → 期限なし → 新着順
  - フィルタ: 都市チップ + 未完了のみトグル
- 同期:
  - `/api/sync-todos` (GET + POST) 新設、ロジックは `/api/sync-itinerary` と同形
  - 純関数 `mergeTodos` (`src/lib/todos-merge.ts`)、last-write-wins per id
  - localStorage 3 キー: cache / queue / last-sync
  - オフライン対応: `online`/`offline` イベントで自動 drain
- 根拠: ユーザー要望 (2026-06-23)「旅行に向けての ToDo を整理したい」
- 代替案 (採用せず):
  - 既存エンティティへの紐付け → 旅程/レストランとの整合性管理コスト高、予約以外の ToDo (両替等) を扱いにくい
  - 新規 Gist 作成 → setup スクリプト再実行、env var 追加で運用負荷
  - localStorage のみ → スマホ ↔ PC 同期不可、本末転倒
- 影響:
  - 新規ファイル 9 個 (`src/types/todo.ts`, `data/todos.json`, `src/data/todo-loader.ts`, `src/lib/todos-merge.ts`, `scripts/test-todos-merge.mjs`, `api/sync-todos.ts`, `src/components/TodoList.astro`, `TodoForm.astro`, `src/pages/todo.astro`)
  - 改変 5 個 (`src/lib/github-gist.ts`, `src/pages/index.astro`, `src/pages/itinerary.astro`, `src/styles/global.css`, `docs/plan/decisions-log.md`)
  - 新規環境変数なし (既存 `ITINERARY_GIST_ID` / `GITHUB_TOKEN` / `ADMIN_PASSWORD` 再利用)

設計書: `docs/superpowers/specs/2026-06-23-todo-design.md`、実装プラン: `docs/superpowers/plans/2026-06-23-todo.md`
```

- [ ] **Step 4: ビルド検証**

Run: `pnpm build`
Expected: PASS — 3 page(s) built

- [ ] **Step 5: Commit**

```bash
git add src/pages/index.astro src/pages/itinerary.astro docs/plan/decisions-log.md
git commit -m "Add /todo nav link + log Phase 8 in D-032"
```

---

## Task 12: E2E 手動検証 (デプロイ後)

**Files:** (なし、操作のみ)

- [ ] **Step 1: push + デプロイ確認**

```bash
git push
```

Vercel デプロイ完了を待つ (~1-2 分)。

- [ ] **Step 2: GET エンドポイント疎通**

PowerShell:

```powershell
$pw = 'あなたの ADMIN_PASSWORD'
curl.exe -H "x-admin-password: $pw" https://italia-gohan-map.vercel.app/api/sync-todos
```

Expected: `{"version":1,"todos":[],"serverTimestamp":"..."}` (初回は空、`todos.json` はまだ Gist に無い)

- [ ] **Step 3: PC ブラウザで動作確認**

1. https://italia-gohan-map.vercel.app/todo を開く
2. ヘッダーに ✓ ToDo / 🔄 / バッジが表示される
3. クイック追加で「コロッセオ予約」「都市: Rome」を入れて Enter
4. ✎ ボタンで開く → パスワード入力 → ロック解除 → 期限 (`2026-08-15`) とメモを入れて保存
5. バッジが「🔄 同期中 (1)」→「✓ 同期済み HH:MM」
6. Gist (https://gist.github.com/shintaro-kawa/b1d5f6ff1992ccd9e64819132f26aba9) を直接確認 → `todos.json` ファイルが新設され、1 件入っている

- [ ] **Step 4: スマホで sync 確認**

1. スマホで https://italia-gohan-map.vercel.app/todo を開く
2. ✎ ボタンで認証 → モーダル閉じる → 🔄 ボタン
3. PC で追加した「コロッセオ予約」が表示される
4. スマホでチェックボックス ON → 完了に
5. PC で 🔄 → 完了状態が反映される

- [ ] **Step 5: 期限視覚化確認**

1. 期限が今日より前 → 🔴 赤バッジ「⚠ N日超過」
2. 期限が今日+3日 → 🟠 オレンジバッジ「⏰ あと3日」
3. 期限が今日+15日 → 通常バッジ「📅 M月D日」
4. 期限なし → バッジ無し

- [ ] **Step 6: フィルタ確認**

1. 都市チップで「Rome」→ Rome のみ表示
2. 「すべて」→ 全件表示
3. 「未完了のみ」OFF → 完了済も表示 (薄字)

- [ ] **Step 7: 削除確認**

1. アイテムの 🗑 → 確認 → 消える
2. Gist `todos.json` を確認 → tombstone (`deletedAt` 立った状態) が残る

- [ ] **Step 8: 完了報告**

問題なければ Task 12 完了。

---

## 完了基準

- [ ] 全 12 タスクのチェックボックスが完了
- [ ] `pnpm build` が型エラー無しで通る
- [ ] `node --experimental-strip-types scripts/test-todos-merge.mjs` が `17 passed, 0 failed`
- [ ] 本番環境で `/api/sync-todos` GET が空 todos を返す
- [ ] PC とスマホで双方向同期、期限視覚化、フィルタ、tombstone 削除が動作
